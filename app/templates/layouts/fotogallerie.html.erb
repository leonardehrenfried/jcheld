<div class="span-24 article-column">
  <div class="photo-gallery">
    <% for photo in articles[0].photos %>
      <div class="photo-box">
        <a class="fancybox" rel="photo-gallery"
           href="<%= image_path(photo.public_filename(:page_width)) %>">
          <%= image_tag(photo.public_filename(:square_thumb)) %>
        </a>
        <div>
          <a href="<%= image_path(photo.public_filename())%>">Hohe Qualität</a>
        </div>
      </div>
    <% end %>
  </div>
</div>
